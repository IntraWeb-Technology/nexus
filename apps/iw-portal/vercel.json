{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --filter=@repo/iw-portal... --filter=my-turborepo",
  "buildCommand": "cd ../.. && pnpm exec turbo run build --filter=@repo/iw-portal",
  "ignoreCommand": "cd ../.. && npx -y turbo@2.9.6 query affected --base=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" --packages @repo/iw-portal --tasks build --exit-code",
  "rewrites": [
    {
      "source": "/:path*",
      "has": [{ "type": "host", "value": "portal.intrawebtech.com" }],
      "destination": "/:path*"
    }
  ]
}
