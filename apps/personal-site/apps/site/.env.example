# Site key: personal (johnschibelli.dev)
# Server-only Strapi — never use NEXT_PUBLIC_ for tokens

# Local CMS: apps/cms-strapi (http://127.0.0.1:1337)
STRAPI_URL=http://127.0.0.1:1337
STRAPI_API_TOKEN=
STRAPI_PREVIEW_SECRET=
STRAPI_WEBHOOK_SECRET=
# Optional gate: set false to force local/Hashnode content even when STRAPI_URL is set
STRAPI_CONTENT_ENABLED=true

# Public site
NEXT_PUBLIC_SITE_URL=https://johnschibelli.dev
NEXT_PUBLIC_HASHNODE_PUBLICATION_HOST=mindware.hashnode.dev

# Blog markdown SoT (if content/blog is empty in this copy):
# sync from portfolio-os apps/site/content/blog
