# `@repo/integrations`

Scaffold for **typed wrappers** around external services. SDK usage remains in apps until an explicit migration.

All `create*Placeholder` functions **throw** if called so they cannot accidentally run in production.
